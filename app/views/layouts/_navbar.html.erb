<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_user %>
        <a class="navbar-brand" href="/dashboard">FileDrop</a>
      <% else %>
        <a class="navbar-brand" href="/">FileDrop</a>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li><%= link_to "My Account", user_path(current_user) %></li>
          <li><%= link_to "My Folders", user_folders_path(current_user) %></li>
          <li><%= link_to "Dashboard", dashboard_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">

          <% if current_user %>
            <li><a href="/users/<%= current_user.id %>">Logged in as <%= current_user.name %></a></li>
            <!-- <li><a href="/logout">Logout</a></li> -->
            <li><%= link_to 'Logout', logout_path, method: :delete %></li>
          <% else %>
            <li><%= link_to 'Login', login_path %></li>
          <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
